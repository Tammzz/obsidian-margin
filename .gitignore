node_modules/

# Build output — shipped as a release asset, not committed.
main.js
main.js.map

# Local plugin state written by Obsidian — user data, not source.
data.json

# Build/editor noise
*.log
.DS_Store

# Local prototype leftover, never committed. Prototype lives in archive/prototype-1.1.0.
main.js.pre-ts-backup
